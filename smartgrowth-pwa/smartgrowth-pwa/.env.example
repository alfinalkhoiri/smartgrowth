# For local web dev, leave this unset (Vite dev server proxies /api).
# For Capacitor builds (native app), this MUST be an absolute URL since
# there's no dev-server proxy inside a native WebView.
VITE_API_BASE_URL=https://your-backend.example.id/api
